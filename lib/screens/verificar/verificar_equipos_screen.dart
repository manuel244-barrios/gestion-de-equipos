import 'package:flutter/material.dart';

class VerificarEquiposScreen extends StatelessWidget {
  const VerificarEquiposScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Verificar Equipos')),
      body: const Center(child: Text('Proceso de Verificación')),
    );
  }
}
